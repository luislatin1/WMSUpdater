<Window x:Class="WMSUpdater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WMS Updater"
        Height="430"
        Width="680"
        Background="#1E1E1E"
        Foreground="#E0E0E0"
        FontFamily="Segoe UI"
        Icon="/Assets/appv2.ico"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <!-- 🔹 RECURSOS VAN AQUÍ, NO EN STACKPANEL -->
    <Window.Resources>

        <!-- Colores -->
        <SolidColorBrush x:Key="BgMain" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="FgMain" Color="#E0E0E0"/>

        <!-- Botones -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#D6970F"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Style.Triggers>

                <!-- HOVER -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Azure"/>
                </Trigger>

                <!-- PRESSED (click) -->
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#505050"/>
                </Trigger>

                <!-- DISABLED -->
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>

            </Style.Triggers>
        </Style>



        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1B1B1C"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontFamily" Value="Consolas"/>
        </Style>

        <!-- ProgressBar -->
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="18"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#D6970F"/>
        </Style>

    </Window.Resources>

    <!-- 🔹 CONTENIDO -->
    <StackPanel Margin="15,15,15,33">

        <!-- HEADER -->
        <StackPanel Orientation="Horizontal"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Margin="0,0,0,15">

            <Image             
            Source="/Assets/LogoStyba.png"
            Width="48"
            Height="48"
            Stretch="Uniform"
            Margin="0,0,12,0"/>

            <TextBlock 
            Text="Actualizador de WMS"
            FontSize="20"
            FontWeight="SemiBold"
            VerticalAlignment="Center"/>
        </StackPanel>
        <!-- Título -->

        <!-- Log -->
        <TextBox x:Name="LogTextBox"
                 Height="200"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- Barra -->
        <ProgressBar x:Name="ProgressBar"
                     Margin="0,12,0,12"/>

        <!-- Botones -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,0,0,15">

            <Button x:Name="UpdateButton"
                    Content="Actualizar WMS"
                    Click="UpdateButton_Click"/>

            <Button x:Name="CloseButton"
                    Content="Cerrar"
                    Click="CloseButton_Click"/>

        </StackPanel>

    </StackPanel>

</Window>
